<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jaro.otwartewniedziele">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:configChanges="orientation|screenSize"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ByAdress" />
        <activity android:name=".MapActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".InputEditShopownerDataActivity" />
        <activity android:name=".LoggedActivity" />
        <activity android:name=".InputShopDataActivity" />
        <activity android:name=".YourShopsActivity" />
        <activity android:name=".DeletedShopInfoActivity" />
        <activity android:name=".EditShopDataActivity" />
        <activity android:name=".UpdateShopInfoActivity" />
        <activity android:name=".AddShopInfoActivity"></activity>
    </application>

</manifest>